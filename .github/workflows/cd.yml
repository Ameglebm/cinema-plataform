name: CD

on:
  push:
    branches: [main]

# Garante que só um deploy roda por vez
concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─────────────────────────────────────────
  # JOB 1 – Build & Push Docker Image
  # ─────────────────────────────────────────
  docker:
    name: Build & Push Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    outputs:
      image: ${{ steps.meta.outputs.tags }}
      version: ${{ steps.meta.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./app
          file: ./app/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─────────────────────────────────────────
  # JOB 2 – Deploy (placeholder)
  # Ativar quando K8s / Terraform estiverem prontos
  # ─────────────────────────────────────────
  # deploy:
  #   name: Deploy to Kubernetes
  #   runs-on: ubuntu-latest
  #   needs: docker
  #   environment: production
  #
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Configure cloud credentials
  #       # Ex: aws-actions/configure-aws-credentials@v4
  #       # ou google-github-actions/auth@v2
  #       # ou azure/login@v2
  #       pass:
  #
  #     - name: Set up kubectl
  #       uses: azure/setup-kubectl@v3
  #
  #     - name: Update K8s deployment image
  #       run: |
  #         kubectl set image deployment/cinema-api \
  #           api=${{ needs.docker.outputs.image }} \
  #           --namespace=cinema
  #
  #     - name: Wait for rollout
  #       run: |
  #         kubectl rollout status deployment/cinema-api \
  #           --namespace=cinema \
  #           --timeout=120s